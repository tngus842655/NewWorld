<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 구글 OAuth 복귀 딥링크 (state/cloud.ts NATIVE_REDIRECT와 동일해야 한다).
                 웹뷰 내 구글 로그인은 차단(disallowed_useragent)이라 시스템 브라우저로 나갔다
                 이 스킴으로 돌아온다 - ROADMAP M5 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.expeditionmonsters.app" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <!-- AdMob 앱 ID - 현재는 구글 공식 테스트 앱 ID (MoneyGame과 같은 단계 방식).
             정식 출시로 승격하기 전에 AdMob 콘솔에서 발급받은 실제 앱 ID로 바꿀 것.
             광고 단위 ID는 여기가 아니라 src/platform/adsConfig.ts에 있다 (같이 바꿔야 함).
             이 meta-data가 없으면 앱이 시작하자마자 죽는다 (SDK가 실행 중 확인함). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 귀환 로컬 알림 (Android 13+, platform/returnAlarms.ts가 런타임에 요청) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
